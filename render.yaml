services:
  - type: web
    name: finance-close-reconciliation-platform
    runtime: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.address 0.0.0.0 --server.port $PORT
    autoDeployTrigger: commit
